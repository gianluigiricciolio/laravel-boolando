export default function setProdPrices() {
    // Refs
    const priceElements = document.querySelectorAll('.price');

    // Check ref
    if (!priceElements) return;

    console.log(priceElements);

}